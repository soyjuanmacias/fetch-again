import uglify from 'rollup-plugin-uglify';
import config from './rollup.config';

config.output = {
    file: 'dist/fetch-again.umd.min.js',
    format: 'umd'
};
config.plugins.push(uglify());
export default config;